Always consult the `archlens` MCP server before making or explaining architectural changes in this repository.

Use `query_decisions` when you need rationale or prior decisions.
Use `get_impact_preview` before proposing structural changes.
